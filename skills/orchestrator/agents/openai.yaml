interface:
  display_name: "Orchestrator"
  short_description: "Route coding work through specialized agents"
  default_prompt: "Use /orchestrator to implement this task through evidence, execution, verification, review, and optional publishing."
policy:
  allow_implicit_invocation: false
